[package]
name = "excel-com-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared JSON protocol types for the Excel COM bridge"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
